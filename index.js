const axios = require('axios')
/*
    //! TODO: ADD DOC COMMENT
    API ENDPOINT = http://localhost:3000/accounts
*/
async function filterUserAccounts(status, dateStr, minBalance) {

    console.log('Hello, World!');

    return 0;
};

module.exports = {
    filterUserAccounts
};